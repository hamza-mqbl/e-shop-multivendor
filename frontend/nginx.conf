server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Single Page App: any unknown route falls back to index.html
    location / {
        try_files $uri $uri/ /index.html;
    }

    # cache built static assets aggressively
    location /static/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
